# /fastpool/homepage/config/services.yaml
# Generated by modules/17_homepage/configure.sh — do not edit manually.

- Core Infrastructure:
    - Gitea:
        icon: gitea.png
        href: https://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8443
        description: Source Control Matrix
        container: homelab_gitea
    - Vaultwarden:
        icon: vaultwarden.png
        href: https://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8444
        description: Encrypted Secret Vault
        container: homelab_vaultwarden
    - AdGuard Home:
        icon: adguard-home.png
        href: http://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8080
        description: Network DNS Sinkhole
        container: homelab_adguard

- Applications & Sync:
    - Immich:
        icon: immich.png
        href: https://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8445
        description: Photo Backup Pool
        container: immich_server
    - Syncthing:
        icon: syncthing.png
        href: https://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8446
        description: Continuous File Sync
        container: syncthing
    - SearXNG:
        icon: searxng.png
        href: https://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8448
        description: Privacy Search Engine
        container: homelab_searxng

- Media & Streaming:
    - Kavita:
        icon: kavita.png
        href: https://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8447
        description: Digital Book Vault
        container: homelab_kavita
    - FreshRSS:
        icon: freshrss.png
        href: http://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8449
        description: RSS Feed Aggregator
        container: homelab_freshrss
    - Wolf Stream:
        icon: /icons/wolf.svg
        href: http://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8585
        description: Low-Latency Display Node
        container: homelab_wolf_den

- Monitoring & Observability:
    - Node Exporter:
        icon: prometheus.png
        href: http://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:9100/metrics
        description: Host System Metrics
        container: homelab_node_exporter
    - cAdvisor:
        icon: cadvisor.png
        href: http://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:9338
        description: Container Resource Metrics
        container: homelab_cadvisor
    - Prometheus:
        icon: prometheus.png
        href: http://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:9091
        description: Metrics Aggregator
        container: homelab_prometheus
    - Promtail:
        icon: loki.png
        href: http://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:9080
        description: Log Aggregation Agent
        container: homelab_promtail

- Network Security & Shares:
    - Samba Daemon:
        icon: /icons/samba.png
        description: "smb://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}/shares"
        container: homelab_samba
    - Snort 3 IDS:
        icon: /icons/snort.png
        description: Intrusion Detection Monitoring
        server: localhost
    - Duplicati:
        icon: duplicati.png
        href: https://${TAILSCALE_HOSTNAME}.${TAILSCALE_TAILNET}:8455
        description: Disaster Recovery & Backup
        container: homelab_duplicati
